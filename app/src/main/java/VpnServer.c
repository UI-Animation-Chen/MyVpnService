#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <string.h>

#include <arpa/inet.h>
#include <netinet/in.h>
#include <sys/socket.h>

static int get_tunnel() {
	int upd_socket_id = socket(AF_INET4, SOCK_DGRAM, );
}

static int get_raw_sock() {
	
}

int main(int argc, char *argv[]) {
	
}

